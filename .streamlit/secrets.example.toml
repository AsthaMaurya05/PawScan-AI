# Copy this file to .streamlit/secrets.toml for LOCAL development, then add
# your real key. (.streamlit/secrets.toml is git-ignored.)
#
# On Streamlit Community Cloud, do NOT commit secrets at all — set them in
# the app's "Advanced settings" in the share.streamlit.io dashboard instead.
#
# Get a free Groq API key at https://console.groq.com (no credit card needed).
GROQ_API_KEY = "gsk_your_key_here"
